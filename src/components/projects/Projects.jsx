import React from "react";
import "./projects.css";
const Projects = ({ id }) => {
  return (
    <div id={id} className="projects-section">
      <h1>Projects</h1>
    </div>
  );
};

export default Projects;
